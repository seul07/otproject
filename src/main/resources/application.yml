spring:
  profiles:
    active:
      - dev
    include :
      - dev

  thymeleaf:
    cache: false

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    database: sql_server
logging:
  level:
    org.hibernate.SQL: debug

